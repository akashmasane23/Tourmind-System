# ── Secrets — NEVER commit ───────────────────
.streamlit/secrets.toml

# ── Python ───────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/

# ── Local data (regenerated) ─────────────────
# Static CSVs SHOULD be committed (they're in the repo)
# Only exclude local-only files
data/itineraries.json

# ── ML Model Files (auto-generated — run: python models/ml_model.py) ──
#models/*.joblib
models/__pycache__/
services/__pycache__/

# ── OS ───────────────────────────────────────
.DS_Store
Thumbs.db